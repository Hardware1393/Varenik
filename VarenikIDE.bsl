// ==========================================
// Varenik IDE v1.0 - Main Launcher
// Координатор всех систем
// ==========================================

Процедура КомандаВыполнитьКод(Кнопка)
    ТекстПрограммы = Объект.ТекстРедактора;
    
    // 1. Запуск Лексера (Файл 1)
    СообщитьСистеме("Varenik: Лексический анализ...");
    Токены = Lexer.Токенизировать(ТекстПрограммы);
    
    // 2. Запуск Парсера (Файл 2)
    СообщитьСистеме("Varenik: Построение дерева AST...");
    Дерево = Parser.СформироватьДеревоВыполнения(Токены);
    
    // 3. Запуск Движка (Файл 3)
    СообщитьСистеме("Varenik: Исполнение в RAM-режиме...");
    VarenikEngine.ЗапуститьVarenik(Дерево);
КонецПроцедуры

Процедура СообщитьСистеме(Текст)
    // Эмуляция терминала Puppy Linux
    Объект.КонсольВывода = Объект.КонсольВывода + Символы.ПС + "> " + Текст;
КонецПроцедуры